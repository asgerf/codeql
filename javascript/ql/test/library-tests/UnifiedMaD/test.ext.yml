extensions:
  - addsTo:
      pack: codeql/javascript-all
      extensible: sourceModel
    data:
      - ['testlib', 'Member[getSource].ReturnValue', 'test-source']
      - ['testlib', 'Member[getSourceInProp].ReturnValue.Member[sourceProp]', 'test-source']
      - ['testlib', 'Member[getSourceInCallback].Argument[0].Parameter[0]', 'test-source']

  - addsTo:
      pack: codeql/javascript-all
      extensible: sinkModel
    data:
      - ['testlib', 'Member[mySink].Argument[0]', 'test-sink']
      - ['testlib', 'Member[mySinkInProp].Argument[0].Member[sinkProp]', 'test-sink']
